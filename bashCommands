1) wc - word count
2) setterm --inversescreen on/off 
3) gpg --encrypt file
4) service ssh restart
5) printf '\033]F32' something like that, to change the color
6) python -m http.server
7) use € alt-gr + 5 to get euro symbol
8) use R-Ctrl with TM to get  ™
9) use R-ctrl with C and o to get ǒ, or e/a with ' to get éá
10)user varr=$(gpg --list-keys | grep 9E9)
11) use echo $varr to display variable
12) Do not export secret key
13) gpg --gen-random 
14) netstat -tupln
15) Let’s now try to decrypt the encrypted file from the previous example:

        > gpg --batch --output greetings1.txt --passphrase mypassword --decrypt greetings.txt.gpg
        gpg: AES256 encrypted data
        gpg: encrypted with 1 passphrase

        This will create the decrypted file greetings1.txt in the same location.

        Note that if we omit the –batch option, the system prompts us to enter the passphrase and then stores it in the session.

        Therefore, to clear the password stored in the session, we can run:

        echo RELOADAGENT | gpg-connect-agent

16) https://ostechnix.com/how-to-check-if-gui-is-installed-in-linux-from-commandline/
    Method 1:

        The first method is to make sure if there is any X session installed on your system. To do 
        so, run: 
	$ ls /usr/bin/*session
        If your Linux system has any GUI session, it should display something like below:
        /usr/bin/dbus-run-session  /usr/bin/gnome-session-custom-session
        /usr/bin/gnome-session

        If a Linux doesn't have any GUI installed in it, you will see an output like below:
        /usr/bin/byobu-select-session  /usr/bin/dbus-run-session

17) usermod -aG sudo noob4 to make a user sudoer
18) Verify if the user is in the sudoers list with command:
	 sudo -l -U noob4
19) sudo gpasswd -a noob4 sudo to add user to sudo group
20) sudo gpasswd -d noob4 sudo to del user from the sudo group.
21) Setting STICKY BIT chmod -t dir-name
22) if the STICKY bit is set but the file is NOT executable then the s is in UPPERCASE, 
	otherwise it is LOWERCASE.
23) chmod u+s or g+s for setuid and setgid bit set.
24) drwxrwx--t 2 root  root  4096 Jan 13 07:58 fold/ folder content not visible but writable file inside
25) echo "$USER `date`" >> nfff/ff
26) the file inside should have w permission only, folder being executable only, no read or write
27) crontab -e for edits, -l for list
28) * * * * * date >> /home/noob4/file_`date`
29) ssh-keygen -t rsa -a 1000
30) copy the pub file to the server, (gen keygen on client), append to the .ssh/authorized_keys file
31) use ssh -i key6.pr noob6@ip-addr to login
32) USE root for .ssh folder and authorized_keys file creation
33) use -P in sftp for port
